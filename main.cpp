#include "head.hpp"
#include "solution.h"
#include "test.h"

int main() {
  test te;
  solution solu;

  //input List
  /*for (int i = 0;i < n;++i) {
    int tmp;
    cin >> tmp;
    p->next = new ListNode(tmp);
    p = p->next;
  }*/
  //output List
  /*p = ans;
  while (p != nullptr) {
    cout << p->val << " ";
    p = p->next;
  }
  cout << endl;*/
}